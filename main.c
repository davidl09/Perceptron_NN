#include "Nodes.h"

struct Network network;

extern long double learning_rate;

int main() {
    srand(time(NULL)); //initialize basics such as seed rand(), create train data array
    long double data[3][BATCH_SIZE];
    long double error;




}
